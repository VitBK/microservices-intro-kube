apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.namespace }}
  name: songs-ms-service
  labels:
    {{- include "songs-service.labels" . | nindent 4 }}
spec:
  selector:
    app: songs-ms
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
